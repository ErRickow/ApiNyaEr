name: telegram message
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: send telegram message on push
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.loh_anjing }}
          token: ${{ secrets.token_tel }}
          format: html
          message: |
            <blockquote><b>🤭New Commits ApiNyaEr</b></blockquote>
            <b><blockquote>😱${{ github.event.commits[0].message }}</blockquote></b>
            <blockquote><b>😤Sha: ${{ github.sha }}</b></blockquote>\n
            <b>me web: <a href="https://errickow.github.io">Click Me</a>
          disable_web_page_preview: True
          disable_notification: true